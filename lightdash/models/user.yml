type: model
name: user
sql_from: 'data-analytics-306119.sfdc.User'
label: 'User'
group_label: 'SFDC'

dimensions:
  - name: id
    type: string
    sql: ${TABLE}.Id
    primary_key: true

  - name: lastname
    type: string
    sql: ${TABLE}.lastname

metrics:
  user_count:
    type: count_distinct
    sql: ${TABLE}.Id
    description: Total unique users
