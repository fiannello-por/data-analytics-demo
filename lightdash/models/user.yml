type: model
name: user
sql_from: 'data-analytics-306119.sfdc.User'
label: 'User'
group_label: 'SFDC'

dimensions:
  - name: id
    type: string
    primary_key: true

  - name: lastname
    type: string

metrics:
  user_count:
    type: count_distinct
    sql: ${TABLE}.id
    description: Total unique users
