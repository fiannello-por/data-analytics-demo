---
title: Governance & Quality
description: Access control, change management, and documentation strategy.
---

import { Callout } from 'fumadocs-ui/components/callout';

<Callout type="info">
  The governance structure described below is currently configured for
  demonstration purposes. Every aspect — folder structure, access roles,
  validation rules, notification channels — can be adapted to match the team's
  actual needs.
</Callout>

## 5.1 Access Control

Access control follows a personal/shared folder model:

```text
Lightdash/
├── Shared/                  ← Production (CI-managed)
│   ├── Dashboards/          ← Only dashboards
│   └── Charts/              ← Only charts
├── <User A>/                ← Personal folder
├── <User B>/                ← Personal folder
└── ...
```

| Folder                  | View           | Edit       | Governed by       |
| ----------------------- | -------------- | ---------- | ----------------- |
| **Personal (own)**      | Everyone       | Owner only | Lightdash roles   |
| **Personal (others)**   | Everyone       | Nobody     | Lightdash roles   |
| **Shared (production)** | Selected users | CI/CD only | GitHub PR process |

- **Personal folders** are for prototyping, experimentation, and ad-hoc analysis. Move fast, break nothing in production.
- **Shared folder** is production. Changes go through pull requests, automated validation, and human review. No manual edits.

## 5.2 Security Considerations

A full security assessment was conducted against the self-hosted Lightdash deployment. The scores below reflect the fully hardened configuration after all pending items are implemented.

**Assessment scope:** Lightdash open-source v0.2628.0 on Render (Professional workspace) + Cloudflare free WAF, connected to BigQuery (GCP project `data-analytics-306119`). Full assessment details: [Issue #35](https://github.com/fiannello-por/data-analytics-demo/issues/35).

### Security Posture — Target State (8.0 / 10)

| #   | Category                            | Score   | Controls in Place                                                                                                                                                                                                                                                   |
| --- | ----------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Authentication & Access Control** | 8.5     | Google OAuth with PKCE + state parameter. Password authentication disabled. Google app set to Internal (only `@pointofrental.com` Workspace accounts). Server-side email domain restriction. 8-hour session max. All unused PATs and non-corporate accounts removed |
| 2   | **Data Encryption**                 | 9.0     | AES-256-GCM for warehouse credentials (random salt/IV per operation). Render Postgres AES-256 at rest. TLS 1.2+ in transit. `LIGHTDASH_SECRET` at 288-bit entropy                                                                                                   |
| 3   | **API Security**                    | 6.5     | Cloudflare WAF rate limiting on all endpoints. Headless browser, embedding, and service accounts disabled. Attack surface minimized to core analytics use                                                                                                           |
| 4   | **SQL Injection Protection**        | 7.0     | Application database (Postgres/Knex) uses parameterized queries. Warehouse SQL uses custom escaping (double quotes, strip comments, null byte removal) across 8+ SQL dialects. Custom SQL dimensions are RBAC-gated                                                 |
| 5   | **Infrastructure Security**         | 8.5     | Render Professional private networking (Lightdash ↔ Postgres). Cloudflare WAF + rate limiting. Secure cookies over HTTPS only. Telemetry disabled. GCP service account confirmed least-privilege (`bigquery.jobUser` + `bigquery.dataViewer` only)                  |
| 6   | **Dependency Health**               | 6.0     | Dependabot monitors weekly for new Lightdash releases. No known CVEs against Lightdash itself. Remaining CVEs are in transitive dependencies managed by Lightdash upstream                                                                                          |
| 7   | **Operational Security**            | 9.0     | Dockerfile pinned to specific version (no auto-deploy). Dependabot PR workflow for controlled upgrades. Incident response runbook. Render Professional automated backups with point-in-time recovery                                                                |
| 8   | **Credential Management**           | 7.5     | PATs reduced to active-use only with 90-day expiry. GCP service account verified least-privilege. `LIGHTDASH_SECRET` confirmed 288-bit entropy                                                                                                                      |
|     | **OVERALL**                         | **8.0** |                                                                                                                                                                                                                                                                     |

### Accepted Risk (Lightdash Codebase)

These exist in **every Lightdash edition**, including Cloud Pro ($2,400/mo). Only the Lightdash development team can address them:

| Issue                                                                               | Risk Level | Mitigation                                                                               |
| ----------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------- |
| Single master secret (`LIGHTDASH_SECRET`) for encryption, sessions, and PAT hashing | Medium     | High-entropy secret (288 bits). Rotation procedure documented in runbook                 |
| PBKDF2 key derivation at 2,000 iterations (OWASP recommends 600,000+)               | Medium     | Only exploitable if encrypted database is exfiltrated. Render Postgres encrypted at rest |
| Warehouse SQL via string concatenation (not parameterized)                          | Medium     | Custom escaping compensates. Custom SQL dimensions restricted via RBAC                   |
| CSV/file download endpoints without authentication                                  | Medium     | Requires valid 21-char random nanoId. No directory listing. Files auto-expire            |
| No CSRF token middleware                                                            | Low        | `SameSite=Lax` cookies active. Embedding disabled — bypass path closed                   |
| No CSV formula injection sanitization                                               | Low        | Internal users only. Awareness-based mitigation                                          |

### Maintenance & DevOps Artifacts

| Artifact                                  | Status    | Purpose                                                                                                                                       | Ongoing Effort                                 |
| ----------------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| **Version-pinned deploy repo**            | Delivered | Forked Dockerfile pinned to v0.2628.0. Prevents unreviewed auto-updates                                                                       | —                                              |
| **Dependabot (weekly Docker monitoring)** | Delivered | Opens PR on Mondays when new Lightdash version is published                                                                                   | ~15 min/week to review changelog + merge       |
| **Incident response runbook**             | To create | 8-scenario playbook with containment, credential rotation (exact CLI commands), and recovery steps                                            | 2–3 hours initial, review quarterly            |
| **Monitoring & alerting**                 | To create | Render log drains → free alerting service (Betterstack or Grafana Cloud free tier). Detect anomalous logins, API abuse, unusual query volumes | 2–4 hours initial, ~30 min/month               |
| **Backup & disaster recovery plan**       | To create | Render Professional Postgres backup schedule + restore procedure + RTO/RPO targets                                                            | 1–2 hours initial (after Professional upgrade) |
| **Quarterly security review**             | To create | Re-run Trivy + Semgrep on new versions before upgrading. Verify GCP SA roles. Review PAT list                                                 | 1 hour/quarter                                 |

### Cloud Pro vs Self-Hosted

|                              | Self-Hosted (target)                   | Cloud Pro            | Difference                                    |
| ---------------------------- | -------------------------------------- | -------------------- | --------------------------------------------- |
| **Security score**           | 8.0                                    | ~8.5                 | +0.5 from ops (their SRE team), not from code |
| **Monthly cost**             | $63                                    | $2,400               | 38x more expensive                            |
| **Annual cost**              | $756                                   | $28,800              | $28,044 difference                            |
| **Codebase security issues** | 8                                      | 8                    | Identical                                     |
| **Authentication**           | Google OAuth + PKCE, domain-restricted | Same                 | Identical                                     |
| **Encryption**               | AES-256-GCM, TLS 1.2+                  | Same                 | Identical                                     |
| **WAF / rate limiting**      | Cloudflare free tier                   | Their infrastructure | Equivalent                                    |

The ~0.5 score difference is operational resilience — their SRE team handles monitoring, incident response, and backups — not stronger security controls. With the maintenance artifacts above in place, the operational gap narrows further.

**Cloud Pro features that are genuinely exclusive** (and not security-related):

| Feature                                                               | Cost                  |
| --------------------------------------------------------------------- | --------------------- |
| AI Agents (natural language data exploration, Slack @mention Q&A)     | +$600/mo add-on       |
| Smart caching (enterprise-gated query result caching)                 | Included in Cloud Pro |
| User groups + private spaces (bulk permission management convenience) | Included in Cloud Pro |
| 30-day version history (vs 3-day)                                     | Included in Cloud Pro |
| Dedicated human support (1-day SLA)                                   | Included in Cloud Pro |

<Callout type="info">
  **Recommendation:** Self-hosted at $63/month achieves 94% of the security
  posture that $28,800/year buys, with identical codebase risk. Per-user access
  control is already available in self-hosted — user groups only add bulk
  management convenience, not new security capabilities.
</Callout>

## 5.3 Change Management & Stakeholder Communication

Every change to production analytics is communicated automatically — no manual effort required.

<BranchFlow
  trigger="PR merged to main"
  branches={[
    { label: 'Production', sublabel: 'auto-deployed' },
    {
      label: 'Changelog',
      sublabel: 'auto-generated → published to changelog site',
    },
    { label: 'Slack', sublabel: 'subscribers notified' },
  ]}
/>

**Subscription model:**

Users can subscribe to the dashboards they care about. When a change affects a subscribed dashboard, the subscriber is mentioned directly in Slack — no noise from changes to dashboards they don't follow.

For example: if User A subscribes to the Sales Performance Dashboard, and a PR modifies the pipeline chart in that dashboard, User A gets a Slack mention with a summary of what changed and a link to the changelog entry.

**Channels:**

- **Slack channel** (e.g., `#analytics-changelog` — placeholder, can be any channel the team prefers): All change notifications flow here. Subscribers are @-mentioned for changes to their dashboards.
- **Changelog site:** A browsable, searchable history of every analytics change. Each dashboard can link directly to its filtered changelog page.

## 5.4 Documentation Strategy

Documentation is not a separate task — it's a byproduct of the architecture:

| Documentation type           | How it's generated                                                                                  | Where it lives               |
| ---------------------------- | --------------------------------------------------------------------------------------------------- | ---------------------------- |
| **Metric definitions**       | Written once in the [semantic layer](https://docs.lightdash.com/guides/how-to-create-metrics/) YAML | Lightdash Explore view + Git |
| **Change history**           | Auto-generated from every merged PR                                                                 | Changelog site               |
| **Review decisions**         | Captured in PR comments and review threads                                                          | GitHub PR history            |
| **Access policies**          | Maintained as Markdown in Git                                                                       | `docs/` directory            |
| **Architecture & standards** | Maintained as Markdown in Git                                                                       | `docs/` directory            |

**Proposed: a dedicated data glossary & documentation portal.**

Beyond auto-generated documentation, we can build a dedicated site that consolidates all analytics knowledge — metric definitions, business terms, architecture docs, and runbooks — into a single, searchable reference.

This documentation can be connected to an AI assistant (such as [NotebookLM](https://notebooklm.google/) or a similar tool) so that anyone on the team can ask questions in natural language — "What's the definition of pipeline created?" or "How do I add a new chart?" — and get answers grounded in our actual documentation and metric definitions.
